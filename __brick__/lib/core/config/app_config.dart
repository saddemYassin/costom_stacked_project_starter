import 'dart:ui';

class AppConfig {
  static const supportedLanguages = [
    'en',
    'fr'
  ];
  static const appName = '{{projectName}}';
  static const supportedLocales = [
    Locale('en', 'US'),
    Locale('fr', 'FR')
  ];
}